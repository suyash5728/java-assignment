//Q.8 Demonstrate the use of trim() to remove any trailing whitespace at the beginning and the end of a string.
const str = "     suyash          ";
const trueStr = str.trim(); // trim method will remove whitespace at beginning and at end of string
//output
console.log(trueStr);